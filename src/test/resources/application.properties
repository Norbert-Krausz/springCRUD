# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration
spring.datasource.driver-class-name=org.testcontainers.jdbc.ContainerDatabaseDriver
# Use Spring Boot's default snake_case physical naming (firstName -> first_name) so the
# entities match the snake_case columns created by the Liquibase migrations.
# (PhysicalNamingStrategyStandardImpl kept names verbatim -> "firstname", which the schema lacks.)
# spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
